/**
 * The entire 3D chess game lives in the single, self-contained `index.html`
 * (Three.js + chess.js + Tween.js loaded from CDN). This component renders
 * nothing — the hidden #root node exists only so the Vite build stays happy.
 */
export default function App() {
  return null;
}
